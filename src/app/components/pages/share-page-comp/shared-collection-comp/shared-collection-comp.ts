import { Component } from '@angular/core'
import { IconComp } from '../../../shared/icon-comp/icon-comp'

@Component({
  selector: 'app-shared-collection-comp',
  imports: [IconComp],
  templateUrl: './shared-collection-comp.html',
  styleUrl: './shared-collection-comp.scss'
})
export class SharedCollectionComp {
  /**
   * Todo:
   *
   * mark already imported collections, but allow re-importing
   *
   *
   *
   *
   */
  // collection = input.required<LearnableBaseCollection>()
  // cards
}
